<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hoti.site.db.dao.MapMapper" >
  
  <delete id="unfollowBookmark" parameterType="map" >
    delete from t_bookmark_follow
    where uid = #{uid,jdbcType=BIGINT}
      and fid = #{fid,jdbcType=BIGINT}
  </delete>
  
  <insert id="followBookmark" parameterType="map" >
    insert into t_bookmark_follow (uid, fid)
    values (#{uid,jdbcType=BIGINT}, #{fid,jdbcType=BIGINT})
  </insert>
  
  <select id="selectBookmarkIdsByUserId" parameterType="java.lang.Long" resultType="Long">
  SELECT fid FROM t_bookmark_follow WHERE uid = #{uid} ORDER BY fid
  </select>
  


  <delete id="unfollowGroup" parameterType="map" >
    delete from t_group_follow
    where uid = #{uid,jdbcType=BIGINT}
      and fid = #{fid,jdbcType=BIGINT}
  </delete>
  
  <insert id="followGroup" parameterType="map" >
    insert into t_group_follow (uid, fid)
    values (#{uid,jdbcType=BIGINT}, #{fid,jdbcType=BIGINT})
  </insert>
  
  <select id="selectGroupIdsByUserId" parameterType="java.lang.Long" resultType="Long">
  SELECT fid FROM t_group_follow WHERE uid = #{uid} ORDER BY fid
  </select>
  
  <select id="countGroupUserById" parameterType="long" resultType="int">
  select count(1) from t_group_follow where fid = #{fid}
  </select>
  
  <delete id="deleteGB" parameterType="map" >
    delete from t_group_bookmark
    where gid = #{gid,jdbcType=BIGINT}
      and bmid = #{bmid,jdbcType=BIGINT}
  </delete>
  
  <insert id="insertGB" parameterType="map" >
    insert into t_group_bookmark (gid, bmid)
    values (#{gid,jdbcType=BIGINT}, #{bmid,jdbcType=BIGINT})
  </insert>
  
  <select id="countGBByGroupId" parameterType="long" resultType="int">
  select count(1) from t_group_bookmark where gid = #{gid}
  </select>
  
</mapper>